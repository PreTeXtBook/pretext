This is the managed directory for *generated* assets of the XSL-FO
smoke-test article (`source/directories/@generated` in
`publication.xml`).  It is empty so far: the smoke test does not yet
have any born-in-source images (`latex-image`, `sageplot`, ...) which
would need generating.  This file just keeps the directory in place.
